<h1>Editing Book</h1>
<%= form_with model: @book, url: update_book_path(@book.id), method: :patch do |f| %>

  <% if @book.errors.any? %>
  エラーが発生しました
  <ul>
    <% book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= f.label :title %><br/>
  <%= f.text_field :title %>
	<br/><br/>
  <%= f.label :body %><br/>
  <%= f.text_area :body %>
  <br/>
	
  <%= f.submit 'Create Book'%>
  <br/>

  <%= link_to "Show", book_path(@book.id) %> | <a href="http://127.0.0.1:8080/books">Back</a></p>
<% end %>
